# `npm test` relies on `node --test` glob expansion (see package.json),
# which requires Node >=21. Enforce the engines constraint so `npm install`
# fails loudly on older Node instead of silently collecting zero tests.
engine-strict=true
